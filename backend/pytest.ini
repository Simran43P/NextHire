[pytest]
# Run from the backend/ directory:  python -m pytest
# The model is always mocked - the suite never contacts Ollama or any API,
# so it is fast, offline, and spends no quota.
asyncio_mode = auto
pythonpath = .
testpaths = tests
addopts = -q --tb=short
